import React from "react";
import { Switch, BrowserRouter } from "react-router-dom";

const Router = () => {
  return (
    <BrowserRouter>
      <Switch>
        
      </Switch>
    </BrowserRouter>
  );
};

export default Router;
